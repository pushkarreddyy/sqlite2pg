# Author Identity & Attribution Rule

- NEVER use the user's real name anywhere (in documentation, code comments, package metadata, licenses, CLI output, generated files, or commit messages).
- ALWAYS use only the user's username: `pushkarreddyy` (or `@pushkarreddyy`).
- This applies globally across all files, tools, outputs, and documentation.
