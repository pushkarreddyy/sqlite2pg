# Project Rules

- **Identity & Attribution**: NEVER use the user's real name anywhere (in code, comments, documentation, package metadata, licenses, CLI output, generated files, or commit messages). Always and exclusively use the username `pushkarreddyy` (or `@pushkarreddyy`).
